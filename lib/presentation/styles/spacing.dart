import 'package:flutter/material.dart';

class AppSpacings {
  static const allPadding = EdgeInsets.all(20);
  static const horizontalPadding = EdgeInsets.symmetric(horizontal: 20);
  static const verticalPadding = EdgeInsets.symmetric(vertical: 24);
}
